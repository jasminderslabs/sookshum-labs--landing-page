---
import Button from "../button/Button.astro";

const { heading, word, para, btnText } = Astro.props;
---

<div
  id={btnText.toLowerCase()}
  class='h-full flex flex-col items-center gap-5 px-4 md:px-0 max-w-96.25 md:max-w-125 xl:max-w-165.25'
>
  <div
    class='flex flex-col justify-between gap-2 md:gap-3.75 xl:gap-6 items-center text-white'
  >
    <Button text={btnText} />
    <h2
      class='font-semibold text-[28px] md:text-[36px] lg:text-[42px] xl:text-5xl text-center shrink-0 hero-heading xl:leading-16.75 capitalize'
    >
      {heading.split("|")[0]}
      <i class='font-light'> {word}</i>
      {heading.split("|")[1]}
    </h2>
    <p
      class='text-center text-[12px] md:text-[13px] lg:text-[14px] xl:text-[18px] w-full text-[#f8f8f8b2] font-light'
    >
      {para}.
    </p>
  </div>
</div>
